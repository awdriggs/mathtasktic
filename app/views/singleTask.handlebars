<div id="wrapper">

	<div id="top-nav">
		<h2>{{user.local.username}}</h2>
		<h3><a href="/logout" class="btn btn-default btn-sm">Logout</a></h3>
		<div id="logo"><img src="../images/logo.png"></div>
	</div>{{!-- end top nav --}}
	
	<br>

<div id="task_details">
	<h2>{{data.title}}</h2>
	<h3>Objective</h3>
	<p>{{data.objective}}

	<h3>Problem</h3>
	<p>{{data.description}}
	
	{{#each data.steps}}
	<h3>Step {{@index}}:</h3>
		<p>{{direction}}</p> <a href='/submit/{{../data._id}}/{{_id}}'><button id="next"></button></a>
		

	{{/each}}
	</div>
</div>